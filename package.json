{
  "name": "moureau",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "newstack start",
    "prebuild": "bun run copy:pitches",
    "build": "newstack build --mode=ssg",
    "predeploy": "bun run build",
    "deploy": "bun run deploy.ts",
    "copy:pitches": "bun run scripts/copy-pitches.ts"
  },
  "dependencies": {
    "@moureau/newstack": "^0.0.76",
    "esbuild-plugin-tailwindcss": "^2",
    "marked": "^18.0.0",
    "tailwindcss": "^4"
  },
  "devDependencies": {
    "@moureau/basebox": "^0.0.35",
    "@types/node": "^25.6.0"
  }
}
